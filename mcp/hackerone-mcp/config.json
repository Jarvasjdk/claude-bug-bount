{
  "mcpServers": {
    "hackerone": {
      "command": "python3",
      "args": ["mcp/hackerone-mcp/server.py"],
      "env": {}
    }
  },
  "_comment": "Public endpoints only — no API key required. Copy into opencode.json mcpServers section."
}
